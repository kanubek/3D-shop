@model List<RealEstate3D.Models.Property>
@{
    ViewData["Title"] = "Карта объектов недвижимости";
}

<div class="map-page">
    <div class="page-header">
        <div class="container">
            <div class="header-content">
                <h1>🗺️ Интерактивная карта</h1>
                <p>Найдите идеальную недвижимость на карте Москвы</p>
                
                <div class="quick-stats">
                    <div class="stat-item">
                        <div class="stat-number">@Model.Count</div>
                        <div class="stat-label">Объектов на карте</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">@Model.Where(p => p.Price < 10000000).Count()</div>
                        <div class="stat-label">До 10 млн ₽</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">@Model.DistinctBy(p => p.Location).Count()</div>
                        <div class="stat-label">Районов</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">@((int)(Model.Average(p => (double)p.Price / p.Area)))</div>
                        <div class="stat-label">₽/м² в среднем</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="map-section">
            <div id="map-container"></div>
        </div>
        
        <div class="map-features">
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">🎯</div>
                    <h3>Умный поиск</h3>
                    <p>Фильтруйте объекты по цене, типу и характеристикам прямо на карте</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">🏠</div>
                    <h3>Детальная информация</h3>
                    <p>Кликните на маркер для просмотра полной информации об объекте</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">🎮</div>
                    <h3>3D Просмотр</h3>
                    <p>Переходите к 3D туру прямо с карты для виртуального осмотра</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">💾</div>
                    <h3>Избранное</h3>
                    <p>Добавляйте понравившиеся объекты в избранное для быстрого доступа</p>
                </div>
            </div>
        </div>
        
        <div class="map-analytics">
            <h2>📊 Аналитика рынка</h2>
            
            <div class="analytics-grid">
                <div class="analytics-card">
                    <h4>Распределение по ценам</h4>
                    <div class="price-chart">
                        @{
                            var priceRanges = new Dictionary<string, int>
                            {
                                ["До 5 млн ₽"] = Model.Count(p => p.Price < 5000000),
                                ["5-10 млн ₽"] = Model.Count(p => p.Price >= 5000000 && p.Price < 10000000),
                                ["10-20 млн ₽"] = Model.Count(p => p.Price >= 10000000 && p.Price < 20000000),
                                ["Свыше 20 млн ₽"] = Model.Count(p => p.Price >= 20000000)
                            };
                        }
                        
                        @foreach(var range in priceRanges)
                        {
                            <div class="price-range-item">
                                <div class="range-label">@range.Key</div>
                                <div class="range-bar">
                                    <div class="range-fill" style="width: @(Math.Max(5, (double)range.Value / Model.Count * 100))%"></div>
                                </div>
                                <div class="range-count">@range.Value</div>
                            </div>
                        }
                    </div>
                </div>
                
                <div class="analytics-card">
                    <h4>Популярные районы</h4>
                    <div class="districts-list">
                        @{
                            var districts = Model.GroupBy(p => p.Location ?? "Неизвестно")
                                                .OrderByDescending(g => g.Count())
                                                .Take(5);
                        }
                        
                        @foreach(var district in districts)
                        {
                            <div class="district-item">
                                <div class="district-name">@district.Key</div>
                                <div class="district-stats">
                                    <span class="district-count">@district.Count() объектов</span>
                                    <span class="district-price">от @district.Min(p => p.Price).ToString("N0") ₽</span>
                                </div>
                            </div>
                        }
                    </div>
                </div>
                
                <div class="analytics-card">
                    <h4>Средняя цена за м²</h4>
                    <div class="avg-price-display">
                        <div class="price-big">@((int)(Model.Average(p => (double)p.Price / p.Area))) ₽/м²</div>
                        <div class="price-trend">
                            <span class="trend-icon">📈</span>
                            <span class="trend-text">+12% за год</span>
                        </div>
                    </div>
                    
                    <div class="price-breakdown">
                        <div class="breakdown-item">
                            <span class="breakdown-label">Квартиры</span>
                            <span class="breakdown-value">@((int)(Model.Where(p => p.PropertyType == "Квартира").DefaultIfEmpty().Average(p => p != null ? (double)p.Price / p.Area : 0))) ₽/м²</span>
                        </div>
                        <div class="breakdown-item">
                            <span class="breakdown-label">Дома</span>
                            <span class="breakdown-value">@((int)(Model.Where(p => p.PropertyType == "Дом").DefaultIfEmpty().Average(p => p != null ? (double)p.Price / p.Area : 0))) ₽/м²</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/interactive-map.js"></script>
    <script>
        // Передаем данные объектов в JavaScript
        const mapProperties = @Html.Raw(Json.Serialize(Model.Select(p => new {
            id = p.Id,
            title = p.Title,
            price = p.Price,
            area = p.Area,
            rooms = p.Rooms,
            propertyType = p.PropertyType,
            imagePath = p.ImagePath ?? "/images/no-image.svg"
        })));

        // Инициализируем карту после загрузки страницы
        document.addEventListener('DOMContentLoaded', function() {
            initInteractiveMap(mapProperties);
            
            // Добавляем анимацию появления карточек аналитики
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('animate-in');
                    }
                });
            }, observerOptions);

            document.querySelectorAll('.analytics-card, .feature-card').forEach(card => {
                observer.observe(card);
            });
        });
    </script>
    
    <style>
        .animate-in {
            animation: slideInUp 0.6s ease-out forwards;
        }
        
        @@keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
}